<div class="container my-5">

  <div class="category-show-card bg-white rounded shadow-sm p-4" style="border-left: 8px solid <%= color_for_category(@category) %>;">
    <h1 class="mb-3" style="color: <%= color_for_category(@category) %>;">
      <%= @category.name.titleize %>
    </h1>

    <p class="text-muted fs-5">
      This section can include a description of the "<%= @category.name.titleize %>" genre or category.
      You might add info about typical shows, popular themes, or anything else relevant.
    </p>

    <%= link_to "← Back to Genres", request.referer || categories_path, class: "btn btn-outline-secondary mt-4" %>
  </div>
</div>
